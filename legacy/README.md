# legacy

The original LJMS: a P2P light queue, written as a library.

Kept for its history, not for use. The current LJMS is one level up: the same
idea refined into a template — one table, no broker, no dependencies, a
specified state machine and a race test.
